server:
  port: 8080
spring:
  application:
    name: "crediyaAutenticacion"
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: "/h2"
  profiles:
    include: null
management:
  endpoints:
    web:
      exposure:
        include: "health,prometheus"
  endpoint:
    health:
      probes:
        enabled: true
cors:
  allowed-origins: "http://localhost:4200,http://localhost:8080"
adapters:
  r2dbc:
    host: "localhost"
    port: 5432
    schema: "public"
    database: "crediya-autenticacion"
    username: "auth-user"
    password: "auth-password"
springdoc:
  swagger-ui:
    path: "/api/doc/swagger-ui.html"
  api-docs:
    path: "/api/doc/api-docs"
scalar:
  url: "http://localhost:8080/api/doc/api-docs"
  path: "/docs"
  enabled: true

security:
  jwt:
    secret: "db3b2c4fd9e8ee696d8e04757a66dbc658f60cfc6126fc269fb415fab1eab6c3"
    issuer: "crediya"
    expiration-time: 3600000